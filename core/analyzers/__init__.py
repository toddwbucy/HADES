"""Language server protocol analyzers for semantic code analysis.

This package provides LSP-based code analysis for HADES codebase ingestion.
The primary consumer is rust-analyzer for Rust projects, but the LSP transport
layer (lsp_client) is general-purpose and works with any LSP-compliant server.
"""
